# Entropy (এনট্রপি) – Decision Tree-এর একটি মৌলিক ধারণা

Machine Learning-এ, বিশেষ করে Decision Tree অ্যালগরিদমে, **Entropy** একটি গুরুত্বপূর্ণ ধারণা।

সহজ ভাষায়:

> Entropy হলো কোনো একটি node-এ ক্লাসগুলোর অনিশ্চয়তা (uncertainty) বা impurity পরিমাপের একটি উপায়।

* সব sample একই class হলে → Entropy = 0 (Pure Node)
* সব class সমানভাবে মিশ্রিত হলে → Entropy সর্বোচ্চ

Entropy ধারণাটি Information Theory থেকে এসেছে।

---

## গাণিতিক সংজ্ঞা

ধরি, কোনো dataset-এ মোট **C** টি class আছে। তাহলে entropy সংজ্ঞায়িত হয়:

$$
H = - \sum_{i=1}^{C} p_i \log_2(p_i)
$$

যেখানে:

* $p_i$ = class i-এর probability
* $C$ = মোট class সংখ্যা

Convention অনুযায়ী:

$$
0 \log_2(0) = 0
$$

কারণ,

$$
\lim_{p \to 0} p \log(p) = 0
$$

Negative চিহ্ন ব্যবহার করা হয় কারণ $0 < p_i < 1$ হলে $\log(p_i)$ negative হয়।

---

## Information বা Surprise ধারণা

Information Theory অনুযায়ী কোনো ঘটনার information:

$$
I(p) = -\log_2(p)
$$

* Rare ঘটনা → বেশি information
* Common ঘটনা → কম information

Entropy হলো এই information-এর গড় মান:

$$
H = \mathbb{E}[I(p)]
$$

অর্থাৎ, distribution থেকে random একটি sample নিলে গড়ে আমরা কতটা "অবাক" হবো।

---

## Binary Classification ক্ষেত্রে

দুটি class থাকলে probability ধরা যাক $(p, 1-p)$।

$$
H(p) = -p \log_2(p) - (1-p) \log_2(1-p)
$$

### কেস ১: সম্পূর্ণ বিশুদ্ধ

যদি $p = 1$:

$$
H = -1 \log_2(1) = 0
$$

### কেস ২: ৫০%-৫০% বিভাজন

যদি $p = 0.5$:

$$
H = -0.5 \log_2(0.5) - 0.5 \log_2(0.5)
$$

যেহেতু:

$$
\log_2(0.5) = -1
$$

তাই:

$$
H = 1
$$

এটি binary entropy-এর সর্বোচ্চ মান।

### কেস ৩: ৭০%-৩০%

$$
H = -0.7 \log_2(0.7) - 0.3 \log_2(0.3)
$$

$$
H \approx 0.88
$$

---

## Multi-Class উদাহরণ

ধরা যাক একটি node-এ 100টি sample:

* Class A: 50
* Class B: 30
* Class C: 20

Probability:

$$
p_A = 0.5, \quad p_B = 0.3, \quad p_C = 0.2
$$

Entropy:

$$
H = -0.5 \log_2(0.5) - 0.3 \log_2(0.3) - 0.2 \log_2(0.2)
$$

$$
H \approx 1.485
$$

সর্বোচ্চ entropy (৩টি class হলে):

$$
H_{max} = \log_2(3) \approx 1.585
$$

---

## Entropy-এর সীমা (Bounds)

সর্বনিম্ন:

$$
H_{min} = 0
$$

সর্বোচ্চ:

$$
H_{max} = \log_2(C)
$$

| Class সংখ্যা | সর্বোচ্চ Entropy |
| ------------ | ---------------- |
| 2            | 1                |
| 3            | 1.585            |
| 4            | 2                |
| 10           | 3.322            |

---

## Decision Tree-তে Entropy

Decision Tree-এর লক্ষ্য হলো impurity কমানো।

এই কমানোর পরিমাণকে বলা হয় **Information Gain (IG)**:

$$
IG = H(parent) - \sum_{k} \frac{n_k}{n} H(child_k)
$$

যেখানে:

* $n$ = parent node-এর মোট sample
* $n_k$ = child k-এর sample সংখ্যা
* $H(child_k)$ = child node-এর entropy

যে split সর্বোচ্চ Information Gain দেয়, সেটিই নির্বাচন করা হয়।

---

## ধাপে ধাপে উদাহরণ

ধরা যাক label:

```
[A, A, B, A, B, B, A, C, A, C]
```

Class count:

* A = 5
* B = 3
* C = 2

Probability:

$$
p_A = 0.5, \quad p_B = 0.3, \quad p_C = 0.2
$$

Entropy:

$$
H = 1.485
$$

---

## Binary Entropy Curve বৈশিষ্ট্য

* $H(0) = 0$
* $H(0.5) = 1$ (Maximum)
* $H(1) = 0$

Curve symmetric এবং concave।

---

## কেন Log Base 2?

Log base 2 ব্যবহার করলে entropy মাপা হয় **bits**-এ।

* 1 bit → 1টি yes/no প্রশ্ন
* 2 bits → 2টি প্রশ্ন

Base পরিবর্তন করলে scale পরিবর্তিত হয়, কিন্তু তুলনামূলক বিশ্লেষণ একই থাকে।

---

## Entropy বনাম Gini

| Entropy         | Gini               |
| --------------- | ------------------ |
| Log ব্যবহার করে | Log ব্যবহার করে না |
| সামান্য ধীর     | দ্রুত              |
| বেশি sensitive  | কিছুটা smooth      |

অনেক implementation-এ (যেমন scikit-learn) default হিসেবে Gini ব্যবহৃত হয়।

---

## 🔗 Cross-Entropy সংযোগ

Neural Network-এ ব্যবহৃত loss:

$$
CE = - \sum_i y_i \log(\hat{p}_i)
$$

যেখানে:

* $y_i$ = true label (one-hot)
* $\hat{p}_i$ = predicted probability

Shannon entropy হলো cross-entropy-এর বিশেষ রূপ যখন:

$$
\hat{p}_i = p_i
$$



